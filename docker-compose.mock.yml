services:
  agent-memory-engine:
    environment:
      EMBEDDING_PROVIDER: mock
      EMBEDDING_DIM: "32"
      MEMORY_DB_PATH: /data/lance_mock
      AME_LOG_DISABLED: "0"
    volumes:
      - ame_lance_mock_data:/data/lance_mock

volumes:
  ame_lance_mock_data:
